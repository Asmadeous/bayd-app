# Current feature

_No feature in progress._

The last completed work is archived under `blueprint/history/`. Run `/feature`
(next unchecked build-plan item), `/feature <n|name>`, or `/fix <description>` to
spec the next piece of work here, then `/implement` to build it.
