Phone booking — needs a callback + manual booking

A customer tried to book without an email, so it couldn't be synced to
SimplyBook automatically. Please call them to confirm and book from your end.

Name:  <%= @cr.contact_name.presence || "(not given)" %>
Phone: <%= @cr.contact_phone.presence || "(not given)" %>

<%= @cr.notes %>

Open it in the dashboard under Callbacks to mark it contacted / booked.
